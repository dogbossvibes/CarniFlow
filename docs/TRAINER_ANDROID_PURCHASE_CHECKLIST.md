# Trainer-Abo Android: manuelle Play-Console- und RevenueCat-Prüfliste

Diese Punkte können nicht vollständig aus dem Repository heraus bestätigt oder repariert werden.

- Trainer-Abonnement ist in Google Play Console aktiv.
- Mindestens ein Base Plan des Trainer-Abos ist aktiv.
- Der aktive Base Plan ist in der Schweiz verfügbar.
- Die Google-Play-Produkt-ID stimmt exakt mit RevenueCat und `features/subscription/plans.ts` überein.
- Das Android-App-Paket ist `com.anyvo.app`.
- Der Tester ist im Closed-Test-Track.
- Der Tester hat den Opt-in-Link des Closed Tests geöffnet.
- Die App wurde über Google Play installiert, nicht per Sideload.
- Das Google-Konto des Testers ist zusätzlich als Lizenztester eingetragen.
- Closed-Test-Teilnahme und Lizenztest sind getrennte Einstellungen.
- Die RevenueCat-Play-Store-App ist mit den korrekten Google-Credentials verbunden.
- Das aktuelle RevenueCat-Offering enthält ein Android-Package für das Trainer-Abo.
- Das RevenueCat-Package referenziert exakt die Trainer-Produkt-ID aus der zentralen App-Konfiguration.
